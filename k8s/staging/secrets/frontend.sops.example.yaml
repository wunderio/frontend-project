---
# Example SOPS-encrypted secret.
#
# USAGE:
#   1. Copy this file to frontend.sops.yaml
#   2. Fill in the real values in stringData
#   3. Encrypt in-place:
#        export SOPS_AGE_RECIPIENTS=age1...
#        sops --encrypt --in-place k8s/staging/secrets/frontend.sops.yaml
#   4. Commit the encrypted file.
#
# IMPORTANT: Never commit the plaintext version of this file.
apiVersion: v1
kind: Secret
metadata:
  name: frontend-project-secrets
type: Opaque
stringData:
  # API_KEY: "..."
  # DATABASE_URL: "..."
